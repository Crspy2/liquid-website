import React from 'react'

type Props = {
    productInfo: Product
}

const ProductCard = ({ productInfo }: Props) => {
  return (
    <div>ProductCard</div>
  )
}

export default ProductCard